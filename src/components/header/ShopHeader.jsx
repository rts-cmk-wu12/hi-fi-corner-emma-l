import './ShopHeader.scss';
import HeaderMenu from './HeaderMenu';

export default function ShopHeader() {

    return (
        <header>
            <p>Shop header</p>
            <HeaderMenu />
        </header>
    )
}